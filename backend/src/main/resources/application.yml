server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/movierama
    username: movierama
    password: movierama
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
    open-in-view: false